# ボーカル・ピッチエディタ サーバー側依存関係
# Phase 1-2: 解析・再合成
numpy
scipy
matplotlib
soundfile      # 音声入出力 (libsndfile)
pyworld        # WORLD: F0 推定(Harvest) + 再合成(cheaptrick/d4c/synthesize)
setuptools<81  # pyworld が実行時に pkg_resources を import する。setuptools 81 で pkg_resources が
               # 削除されたため 80 系に固定（Python3.12 の venv には setuptools 自体が既定で入らない）

# Phase 3: API サーバー
fastapi
uvicorn[standard]
python-multipart  # multipart/form-data（音声アップロード）

# テスト（任意）
pytest
httpx             # FastAPI TestClient 用

# 備考: F0 推定は当初 librosa(pyin) を想定していたが、依存の numba/llvmlite が
# LLVM ビルドを要し導入が重いため、WORLD Harvest に一本化した。Phase 2 の
# 再合成と同じ基盤で扱える利点もある。torchcrepe 等へ差し替える場合は
# analysis.estimate_f0 のみ変更すればよい。
